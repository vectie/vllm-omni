# Atlas 800I A3 / Ascend 910C experimental last-block-to-Euler profile.
#
# Extend the accepted prompt-width profile by absorbing the final streaming
# DiT block's Conv+MLP residual, final AdaLN/projection, CFG reduction, and
# Euler update into its existing static GE replay. Startup validates the fused
# result against the accepted split graph with a strict maximum-drift gate;
# any compile or runtime failure falls back to that accepted path.

base_config: minicpmo_4_5_2npu_910c_cfm6_hift_f0_graph_experimental.yaml

connectors:
  connector_of_shared_memory:
    name: SharedMemoryConnector
    extra:
      connector_get_sleep_s: 0.01
      connector_get_max_wait_first_chunk: 3000
      connector_get_max_wait: 300
      codec_chunk_frames: 25
      codec_left_context_frames: 3
      raw_tensor_shm: false
      shm_event_notifications: true
      shm_notification_namespace: minicpmo45-910c
      token2wav_n_timesteps: 6
      npu_dit_mlp_graph: true
      npu_dit_mlp_graph_width: 50
      npu_dit_graph_buckets: [20, 302]
      npu_dit_preamble_graph: true
      npu_dit_wide_adaln: true
      npu_dit_wide_final_adaln: true
      npu_dit_final_addcmul: true
      npu_dit_conv_mlp_graph: true
      npu_dit_prompt_conv_mlp_graph: true
      npu_dit_fused_conv_pack: true
      npu_dit_last_block_final_euler_graph: true
      npu_single_request_cache_passthrough: true
