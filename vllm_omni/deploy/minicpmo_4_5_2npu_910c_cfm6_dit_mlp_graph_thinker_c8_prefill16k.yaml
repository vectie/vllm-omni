# Atlas 800I A3 / Ascend 910C Thinker c8 + 16K prefill candidate.
#
# Daily-Omni and Video-MME prompts are dominated by multimodal prefill. This
# overlay doubles only the Stage 0 scheduler token budget so a later A/B can
# measure whether fewer 8K prefill slices outweigh the larger instantaneous
# NPU working set. Admission, decoding, Talker, and Code2Wav stay unchanged.

base_config: minicpmo_4_5_2npu_910c_cfm6_dit_mlp_graph_thinker_c8.yaml

stages:
  - stage_id: 0
    max_num_batched_tokens: 16384

platforms:
  npu:
    stages:
      - stage_id: 0
        max_num_batched_tokens: 16384
